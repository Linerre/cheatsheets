\message{ !name(cheatsheet.tex)}\documentclass[8pt,a4paper,landscape]{extarticle}
\input{preamble}

\begin{document}

\message{ !name(cheatsheet.tex) !offset(259) }
transfers are implicitly performed
\item minimize data transfers between global memory and device by maximizing use of on-chip memory: shared memory and caches
\item
\end{itemize}
\subsection*{3.Optimize instruction usage to achieve max instruction throughput}

\message{ !name(cheatsheet.tex) !offset(260) }

\end{document}
